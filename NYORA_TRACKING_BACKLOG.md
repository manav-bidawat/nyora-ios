- [x] TS-001 | Backend: add nyora_tracking table | repo:server | ✓ Added Tracking SQLAlchemy model (PK user_id+manga_id+tracker_id, canonical columns) + registered in TABLES; py_compile PY_OK. In /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-sync-server/app/models.py add a SQLAlchemy Tracking model: table "nyora_tracking", PK (user_id, manga_id, tracker_id); columns: remote_id(str), source_id(str), title(str), status(str), score(float), last_read_chapter(float), last_read_volume(int), total_chapters(int), total_volumes(int), chapter_offset(int), started_at(str ISO), finished_at(str ISO), comment(str), updated_at(str ISO), deleted_at(str ISO nullable). Register it in the TABLES dict (~line 159). No endpoint changes (the sync endpoint is generic). Verify: cd /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-sync-server && python3 -m py_compile app/*.py && echo PY_OK. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-sync-server.
- [x] TS-002 | Unified tracking schema spec | repo:aidoku | ✓ Wrote NYORA_TRACKING_SCHEMA.md: canonical record, iOS TrackStatus(1-6)<->Android ScrobblingStatus(name)<->canonical status mapping, tracker_id slug<->ScrobblerService int mapping, per-platform field sources + LWW/soft-delete rules. Doc-only. Write /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku/NYORA_TRACKING_SCHEMA.md documenting the canonical tracking record (fields above) + the status-enum mapping between iOS TrackStatus (reading/planning/completed/paused/dropped/rereading) <-> Android ScrobblingStatus (READING/PLANNED/COMPLETED/ON_HOLD/DROPPED/RE_READING) <-> canonical string, and tracker_id normalization (iOS string ids <-> Android ScrobblerService ints). No build needed; commit in /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku.
- [x] TS-003 | iOS: sync tracking (push/pull nyora_tracking) | repo:aidoku | ✓ Added pushTracking()/pullTracking() to NyoraSyncClient.swift: push serializes CoreData TrackObject link + live TrackState (status via canonical map, score/chapter/volume/totals/dates) into full canonical snake_case nyora_tracking rows with updated_at (skips rows whose live state fetch fails to avoid clobbering); pull restores missing TrackObject links for recognized trackers + honors deleted_at tombstones; wired both into syncNow(). BUILD SUCCEEDED. In /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku/iOS/Sync/NyoraSyncClient.swift add pushTracking()/pullTracking() mapping the CoreData Track (TrackObject: id=remote_id, trackerId, sourceId, mangaId, title, chapterOffset) plus fetched TrackState (status/score/lastReadChapter/lastReadVolume/totals/dates) <-> nyora_tracking rows (canonical schema, snake_case), with updated_at + LWW; wire into syncNow(). Build: cd /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku && xcodebuild -project Aidoku.xcodeproj -scheme "Aidoku (iOS)" -sdk iphonesimulator -configuration Debug -destination 'generic/platform=iOS Simulator' -clonedSourcePackagesDirPath /tmp/aidoku-spm -skipPackagePluginValidation build 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:" | sort -u. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku.
- [x] TS-004 | iOS: close sync coverage gap | repo:aidoku | ✓ Added push+pull for nyora_history, nyora_category, nyora_manga_category, nyora_source_prefs (pin state), nyora_manga_prefs (per-manga reading-mode override) to NyoraSyncClient.swift, matching server columns + mirroring the Android client (composite manga_id, LWW updated_at, deleted_at tombstones). nyora_bookmark intentionally NOT synced from iOS (no page-bookmark model on iOS; Android owns that table). syncNow() reordered to pull-then-push across all tables. BUILD SUCCEEDED. NyoraSyncClient currently syncs ONLY nyora_manga + nyora_favourite. Add push+pull for nyora_history, nyora_bookmark, nyora_category, nyora_manga_category, nyora_manga_prefs, nyora_source_prefs (match the server columns from app/models.py; mirror the Android/desktop clients). Wire into syncNow(). Build: cd /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku && xcodebuild -project Aidoku.xcodeproj -scheme "Aidoku (iOS)" -sdk iphonesimulator -configuration Debug -destination 'generic/platform=iOS Simulator' -clonedSourcePackagesDirPath /tmp/aidoku-spm -skipPackagePluginValidation build 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:" | sort -u. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku.
- [x] TS-005 | iOS: backup carries full tracking state | repo:aidoku | ✓ Extended BackupTrackItem with canonical state fields (status/score/lastReadChapter/lastReadVolume/totalChapters/totalVolumes/startedAt/finishedAt, all optional for backward compat) + init(trackObject:state:) + trackState/trackUpdate accessors. createBackup now pre-fetches live TrackState (async, keyed trackerId|remoteId) via TrackerManager for logged-in trackers and embeds it. Restore writes the backed-up state back to each logged-in tracker service (tracker.update) so a restore re-populates remote state and feeds sync. BUILD SUCCEEDED. Extend the iOS backup (Shared/Data/Backup/Models/BackupTrackItem.swift + BackupManager) so tracking backup includes status/score/progress/dates (the canonical fields), not just the link, so a restore feeds sync. Build: cd /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku && xcodebuild -project Aidoku.xcodeproj -scheme "Aidoku (iOS)" -sdk iphonesimulator -configuration Debug -destination 'generic/platform=iOS Simulator' -clonedSourcePackagesDirPath /tmp/aidoku-spm -skipPackagePluginValidation build 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:" | sort -u. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku.
- [x] TS-006 | Android: sync tracking (push/pull nyora_tracking) | repo:android | ✓ Added pushTracking()/pullTracking() + SbTracking DTO (full canonical snake_case row) to SupabaseSync.kt over ScrobblingDao/ScrobblingEntity: scrobbler int<->tracker_id slug (§3), service-specific stored status string<->canonical status via per-service maps (§2, entity stores API status not enum name), target_id<->remote_id, chapter<->last_read_chapter, rating<->score, comment; entity `id` is a local surrogate (not synced), reconstructed on pull via find() else remote_id; deleted_at tombstones remove local link; wired into pushAll/pullAll. BUILD SUCCESSFUL. In /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-android/app/src/main/kotlin/com/nyora/hasan72341/sync/supabase/SupabaseSync.kt add pushTracking/pullTracking over ScrobblingDao/ScrobblingEntity (table scrobblings: scrobbler->tracker_id, id, manga_id, target_id->remote_id, status, chapter->last_read_chapter, rating->score, comment) <-> nyora_tracking; add an SbTracking DTO; wire into pushAll/pullAll. Build: cd /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-android && ./gradlew :app:compileDebugKotlin 2>&1 | grep -E "BUILD SUCCESSFUL|BUILD FAILED|e: " | head -20. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-android.
- [x] TS-007 | Desktop backup: full canonical + tracking | repo:shared | ✓ Rewrote handleBackupExport/handleBackupImport in NyoraRestServer.kt to round-trip ALL canonical entities (favourites, history, categories + manga↔category membership, bookmarks, mangaPrefs, sourcePrefs pin state) plus a canonical snake_case tracking section (matches server nyora_tracking + iOS/Android). Envelope bumped to version 2. Added TrackingRow (canonical fields) + allTracking()/saveTracking() default hooks to LibraryRepository (populated by TS-010's store) and facade passthroughs allMangaPrefs/allTracking/saveTracking. Import is idempotent (category match-by-title with backup→local id remap, bookmark de-dup, pin toggle only on diff). Built via nyora-linux :shared:compileKotlinJvm — BUILD SUCCESSFUL. In /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared/src/jvmMain/kotlin/com/nyora/hasan72341/shared/proxy/NyoraRestServer.kt fix handleBackupExport/handleBackupImport (currently lossy favourites+history only) to export/import all canonical entities (favourites, history, categories, bookmarks, manga_prefs, source_prefs) AND a tracking section, round-tripping without loss. Build: discover + run the gradle compile for nyora-shared (e.g. ./gradlew compileKotlinJvm or the jvm target); if you cannot get a build to run, BLOCK. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared.
- [x] TS-008 | Desktop: scrobbler abstraction in nyora-shared | repo:shared | ✓ Added self-contained JVM scrobbling package under shared/jvmMain (com.nyora.hasan72341.shared.scrobbling): ScrobblerService enum (int id + canonical slug + Android's real client IDs/secrets from constants.xml), ScrobblingStatus (with canonical mapping), models (ScrobblerManga/MangaInfo/User/ScrobblingInfo + pluggable ScrobblerTokenStore), abstract Scrobbler base (OkHttp+kotlinx.serialization, search/getState/updateProgress/getMangaInfo/oauthUrl/authorize + status<->wire mapping + JSON helpers), and real per-service impls AniListScrobbler(GraphQL)/MalScrobbler(REST+PKCE)/KitsuScrobbler(JSON:API password grant)/ShikimoriScrobbler(REST v2 + User-Agent), plus a ScrobblerRepository registry (mirrors Android ScrobblerRepositoryMap). Ratings normalized to 0..1 per service scale. Built via nyora-linux :shared:compileKotlinJvm --rerun-tasks — BUILD SUCCESSFUL. Port a Scrobbler abstraction into /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared jvmMain (mirror Android's scrobbling/common/domain/Scrobbler.kt + ScrobblerRepository): interfaces + per-service impls for AniList, MyAnimeList, Kitsu, Shikimori (search + updateProgress + getState). Reuse Android's real client IDs from /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-android/app/src/main/res/values/constants.xml. Build the shared jvm target; BLOCK if unbuildable. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared.
- [x] TS-009 | Desktop: server endpoints for MAL/Kitsu/Shikimori | repo:shared | ✓ Added /tracker/{myanimelist,kitsu,shikimori}/{search,scrobble} handlers to NyoraRestServer.kt as thin bearer-token passthroughs (mirroring the existing /tracker/anilist/* pattern): each forwards ONE request to the upstream service API with the client-supplied Authorization: Bearer token and streams the raw JSON back. MAL=REST v2 (GET /manga?q, PUT /manga/{id}/my_list_status form body); Kitsu=JSON:API (GET /manga?filter[text], POST create or PATCH library-entries with vnd.api+json + relationships/attributes); Shikimori=REST v2 (GET /mangas?search + User-Agent, POST/PATCH /api/v2/user_rates). Added a shared serviceRequest() helper (GET/POST/PUT/PATCH; PATCH tunnelled via POST + X-HTTP-Method-Override since HttpURLConnection rejects PATCH) + bearerOrNull()/urlEncode(). Built via nyora-linux :shared:compileKotlinJvm — BUILD SUCCESSFUL. In NyoraRestServer.kt add /tracker/{myanimelist,kitsu,shikimori}/{search,scrobble} handlers (mirror the existing /tracker/anilist/{search,scrobble} at ~lines 1516-1593) as bearer-token passthroughs to each service API. Build shared jvm target; BLOCK if unbuildable. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared.
- [x] TS-010 | Desktop: local tracking store | repo:shared | ✓ Added SQLDelight `tracking` table (Tracking.sq, PK tracker_id+manga_id, all canonical columns; dates/updated_at/deleted_at stored as ISO TEXT for lossless round-trip; selectAll/selectLive/selectForManga/upsert/softDelete/deleteByKey/countLive/deleteAll queries + manga_id index). Implemented allTracking()/saveTracking() overrides in SqlDelightLibraryRepository (maps generated Tracking row <-> canonical TrackingRow, upsert with LWW updated_at fallback, triggerPush) plus trackingForManga()/removeTracking() helpers; wired tracking into hasLocalSyncableData() (countLive) and clearDatabase(). Built via nyora-linux :shared:compileKotlinJvm (generateCommonMainNyoraDatabaseInterface + compileKotlinJvm) — BUILD SUCCESSFUL. Add a local tracking store to nyora-shared (SQLDelight, alongside the existing library repo) persisting per-service link + state (tracker_id, remote_id, manga_id, source_id, title, status, score, last_read_chapter, dates, updated_at, deleted_at) matching the canonical schema. Build; BLOCK if unbuildable. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared.
- [x] TS-011 | Desktop: OAuth flows (loopback) | repo:shared | ✓ Made Scrobbler.redirectUri overridable (new open var + defaultRedirectUri; AniList/MAL/Shikimori now URL-encode redirectUri in oauthUrl and use it in the token exchange; Kitsu keeps its password grant). Added OAuthLoopbackServer (com.sun.net.httpserver on 127.0.0.1, captures code/error/state, serves a close-window page, awaitCode with timeout), ScrobblerOAuth.login() (spins loopback on FIXED_LOOPBACK_PORT 43217, points redirectUri at it, opens system browser via AWT Desktop w/ CLI fallback, appends CSRF state, awaits code, calls authorize) + loginWithPassword() for Kitsu, and PersistentScrobblerTokenStore (disk-backed Properties in the Nyora data dir, keyed <slug>.access/refresh_token) wired via ScrobblerRepository.persistent(). Code comments note loopback/deep-link redirect URIs may need per-service registration. Built via nyora-linux :shared:compileKotlinJvm — BUILD SUCCESSFUL. Implement OAuth login for AniList/MAL/Kitsu/Shikimori in nyora-shared using a loopback (http://127.0.0.1:<port>) or nyora:// deep-link redirect + token exchange + token storage, reusing Android's client IDs. NOTE in code comments that redirect URIs may need external registration per service. Build; BLOCK if unbuildable. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared.
- [ ] TS-012 | Desktop: sync tracking | repo:shared | In /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared/src/jvmMain/kotlin/com/nyora/hasan72341/shared/sync/SupabaseSync.kt add pushTracking/pullTracking + an SbTracking DTO over the new local tracking store <-> nyora_tracking, wired into pushAll/pullAll (LWW). Build; BLOCK if unbuildable. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-shared.
- [ ] TS-013 | mac: multi-service tracker UI + OAuth | repo:mac | Generalize mac tracker UI (/Users/hasanraza/Desktop/kotatsu/Nyora/nyora-mac/macApp/Nyora/NyoraApp/ViewModels/TrackerSettings.swift + Views/PlaceholderViews.swift AniList panel) to list AniList/MAL/Kitsu/Shikimori with OAuth login (via the shared flows / helper endpoints) + per-service state. Build: discover + run the mac app build (xcodebuild or swift build under /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-mac); BLOCK if unbuildable. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-mac.
- [ ] TS-014 | linux/windows: multi-service tracker UI + OAuth | repo:linux | Generalize the Compose desktop tracker UI (/Users/hasanraza/Desktop/kotatsu/Nyora/nyora-linux/desktopApp/.../ui/screen/TrackerScreen.kt, shared with windows) from AniList-only to multi-service (AniList/MAL/Kitsu/Shikimori) with OAuth. Build: discover + run the Compose Desktop gradle compile under /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-linux; BLOCK if unbuildable. Commit in /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-linux (and /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-windows if it has separate UI code).
