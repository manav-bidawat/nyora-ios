# Nyora iOS UI Fix Backlog

- [x] FX-001 | Discover: keep ONE search bar | ✓ Removed the .toolbar magnifying-glass ToolbarItem; only the inline "Search all sources" pill (searchBar) remains, both calling openUniversalSearch(). | Discover has TWO search entry points — a toolbar magnifying-glass Button (ToolbarItem .topBarTrailing in DiscoverView.swift ~line 59-67) AND an inline "Search all sources" pill (searchBar, ~line 178). Remove the TOOLBAR search button; keep ONLY the inline "Search all sources" bar (android style). Both call openUniversalSearch(); just delete the .toolbar search item.
- [x] FX-002 | Discover: Trending pager dots overflow | ✓ Capped DiscoverRecommendationPager to 8 pages via a `pages` computed prop (manga.prefix(8)); TabView + DotsIndicator both drive off `pages` so the dot count always matches the shown cards. | The DiscoverRecommendationPager shows one dot per trending item (13+), making a huge dot row with a single visible card. Cap the pager to ~8 items max and ensure the DotsIndicator count matches the shown items so it reads cleanly.
- [x] FX-003 | Explore/Browse: remove duplicate search bar | ✓ Removed the decorative NyoraSearchHeroView pill from the Browse table header (property, onTap wiring, header subview, and layout sizing in BrowseViewController.swift); the native UISearchController search bar remains the single working search field. Quick-actions card header now sizes to just quickActionsHeight. | The Explore/Browse screen shows TWO "Search" fields — the app's native search bar AND the design-port "search-hero" pill card (ND-009, iOS/New/Views/Browse/ item_explore_search_hero equivalent). Remove the redundant one; keep a SINGLE working search field on Browse.
- [x] FX-004 | Explore: wire the 4 quick-action buttons | ✓ All four QuickActionsCard tiles route to real destinations via BrowseViewController closures: Local Files → local source (or add-source page if not installed), Bookmarks → Library/Favourites tab, Downloads → DownloadQueueView. Fixed Random to open a random MANGA (fetch getSearchMangaList from a random installed source, push MangaViewController) instead of just a random source, with a fallback to opening the source. | The quick-actions card (Local Files, Bookmarks, Random, Downloads — the ND-010 2x2 card in Browse) buttons do nothing. Wire each to its real destination: Local Files → the local/on-device source or file browser; Bookmarks → bookmarks list; Random → open a random manga from installed sources; Downloads → downloads/download-queue screen. Reuse existing Aidoku screens/actions.
- [x] FX-005 | History: fix blank thumbnails | ✓ HistoryEntryCell's cover now receives the resolved source (SourceManager.shared.source(for: entry.sourceKey)) passed through to MangaCoverView → SourceImageView, so covers get the source's modified image request (Referer/UA headers) exactly like Library/Browse instead of a bare request that fails and shows blank. | History rows (HistoryView, iOS/New/Views/History/) show blank cover thumbnails. Fix cover loading — ensure the history entry's cover URL is proxied/resolved the same way Library/Browse covers are (SourceImageView with the correct source + proxied URL).
- [ ] FX-006 | Replace "More" tab with a Settings tab | The 5th nav destination is "More" (a list containing Settings + Search). User wants the tab to BE Settings (open Settings directly, labelled Settings), NOT a More container. In TabBarController.swift / NavConfig / NavSection: make the settings section the tab (it already exists as .settings) and REMOVE the "More" intermediary list + the redundant in-More Search row (universal search is already on Discover). Tab label "Settings", gear icon.
- [ ] FX-007 | Remove AMOLED option | Remove the "AMOLED Dark Theme" toggle + its footer from Settings → Appearance (find AppearanceView / the amoled Setting). Also remove the now-dead Appearance.amoled default/usages if trivial; otherwise just hide the UI row.
- [ ] FX-008 | Fix double back button on Settings→Appearance | Settings → Appearance shows TWO back chevrons stacked (two nav bars). Caused by nested navigation (the Settings tab wraps SettingsView in a NavigationStack in setUpTabs, and AppearanceView / a pushed view adds ANOTHER NavigationView/Stack or its own nav bar). Remove the redundant nav wrapper so only ONE back button shows. Verify other Settings sub-screens still navigate + have a single back button.
