import Foundation

/// TyrantScans — Ported automatically.
public final class TyrantScans: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "TYRANTSCANS", title: "TyrantScans", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.tyrantscans.com"
        )
    }
}

/// DuoScanlators — Ported automatically.
public final class DuoScanlators: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "DUOSCANLATORS", title: "DuoScanlators", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "duoscanlators.blogspot.com"
        )
    }
}

/// HeckScans — Ported automatically.
public final class HeckScans: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "HECKSCANS", title: "HeckScans", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "heckscans.blogspot.com"
        )
    }
}

/// ElevenScanlator — Ported automatically.
public final class ElevenScanlator: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "ELEVENSCANLATOR", title: "ElevenScanlator", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "elevenscanlator.blogspot.com"
        )
    }
}

/// TemakiMangas — Ported automatically.
public final class TemakiMangas: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "TEMAKIMANGAS", title: "TemakiMangas", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "temakimangas.blogspot.com"
        )
    }
}

/// YaoiFanClub — Ported automatically.
public final class YaoiFanClub: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "YAOIFANCLUB", title: "YaoiFanClub", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.yaoifanclub.com"
        )
    }
}

/// AnimeXNovel — Ported automatically.
public final class AnimeXNovel: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "ANIMEXNOVEL", title: "AnimeXNovel", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.animexnovel.com"
        )
    }
}

/// RaysScan — Ported automatically.
public final class RaysScan: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "RAYSSCAN", title: "RaysScan", locale: "pt")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "raysscan.blogspot.com"
        )
    }
}

/// LonerTranslations — Ported automatically.
public final class LonerTl: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "LONERTL", title: "LonerTranslations", locale: "ar")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "loner-tl.blogspot.com"
        )
    }
}

/// XsanoManga — Ported automatically.
public final class XsanoManga: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "XSANOMANGA", title: "XsanoManga", locale: "ar")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.xsano-manga.com"
        )
    }
}

/// MangaSoul — Ported automatically.
public final class MangaSoul: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "MANGASOUL", title: "MangaSoul", locale: "ar")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.manga-soul.com"
        )
    }
}

/// MangaAiLand — Ported automatically.
public final class MangaAiLand: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "MANGAAILAND", title: "MangaAiLand", locale: "ar")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "manga-ai-land.blogspot.com"
        )
    }
}

/// YokaiTeam — Ported automatically.
public final class YokaiTeam: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "YOKAITEAM", title: "YokaiTeam", locale: "ar")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "yokai-team.blogspot.com"
        )
    }
}

/// ManhaTok — Ported automatically.
public final class ManhaTok: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "MANHATOK", title: "ManhaTok", locale: "ar")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "manhatok.blogspot.com"
        )
    }
}

/// UlasComic — Ported automatically.
public final class UlasComic: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "ULASCOMIC", title: "UlasComic", locale: "id")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.ulascomic01.xyz"
        )
    }
}

/// OkyyKomik — Ported automatically.
public final class OkyyKomik: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "OKYYKOMIK", title: "OkyyKomik", locale: "id")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.okyykomik.my.id"
        )
    }
}

/// ReYume — Ported automatically.
public final class ReYume: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "REYUME", title: "ReYume", locale: "id")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.re-yume.my.id"
        )
    }
}

/// NgamenKomik — Ported automatically.
public final class NgamenKomik: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "NGAMENKOMIK", title: "NgamenKomik", locale: "id")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "ngamenkomik05.blogspot.com"
        )
    }
}

/// ShiyuraSub — Ported automatically.
public final class ShiyuraSub: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "SHIYURASUB", title: "ShiyuraSub", locale: "id")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "shiyurasub.blogspot.com"
        )
    }
}

/// Lepoytl — Ported automatically.
public final class Lepoytl: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "LEPOYTL", title: "Lepoytl", locale: "id")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "www.lepoytl.cloud"
        )
    }
}

/// KomikRealm — Ported automatically.
public final class KomikRealm: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "KOMIKREALM", title: "KomikRealm", locale: "id")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "komikrealm.org"
        )
    }
}

/// DatgarScanlation — Ported automatically.
public final class DatgarScanlation: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "DATGARSCANLATION", title: "DatgarScanlation", locale: "es")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "datgarscanlation.blogspot.com"
        )
    }
}

/// GistamisHouseFansub — Ported automatically.
public final class GistamisHouseFansub: ZeistMangaParser, @unchecked Sendable {
    public static let descriptor = MangaParserSource(name: "GISTAMISHOUSEFANSUB", title: "GistamisHouseFansub", locale: "es")
    public init(context: MangaLoaderContext) {
        super.init(
            context: context,
            source: Self.descriptor,
            defaultDomain: "gistamishousefansub.blogspot.com"
        )
    }
}

