# Nyora Batch Backlog

- [x] NX-001 | Theme/accent fix — presets & picker actually apply app-wide | area:ios | ✓ Added reactive AccentManager ObservableObject (republishes on .accentColorChanged) + NyoraAccentTint root wrapper; fixed .default accent to resolve to indigo (was systemPink); replaced accent-role NyoraTheme.indigo/Color.nyoraIndigo with dynamic accent across TabBar (live reconfigure), Discover, pill button, cards, reader toolbar, grid cell. BUILD SUCCEEDED. | The accent picker + 10 presets don't apply. ROOT CAUSE: (a) SwiftUI `Color.accentColor`/`.accentColor` reads the STATIC asset (fixed indigo) so preset changes never affect SwiftUI screens; (b) the design port HARDCODED NyoraTheme.indigo / Color.nyoraIndigo as the accent in restyled screens (tab bar, Discover, cards) so they ignore the selected accent. FIX: add a reactive dynamic accent the whole app follows — an ObservableObject (e.g. AccentManager: @Published var color, updated on Notification .accentColorChanged reading AccentColor.current) injected via .tint()/.environment at the SwiftUI roots (the UIHostingControllers / app entry), AND replace ACCENT-ROLE uses of NyoraTheme.indigo/Color.nyoraIndigo with the dynamic accent (AccentColor.current.color). Keep the existing window.tintColor wiring (AccentColorApplier in SceneDelegate). ACCEPTANCE: picking any preset (Miku etc.) changes tab/Discover/button tint live + persists across relaunch; default stays indigo.
- [x] NX-002 | AniList feed for Discover (replaces ComicK-first) | area:ios | ✓ Added auth-free AniListClient (POST graphql.anilist.co, TRENDING_DESC + POPULARITY_DESC, maps media→AidokuRunner.Manga with synthetic sourceKey "anilist", HTML-stripped description, open-CDN covers). Rewired DiscoverView: hero = top trending, "Trending" recommendation pager, "Popular" rail (no more source getHome). Made hero/rail/pager cards take source:Source? + onSelect closure. Tapping an AniList entry presents NyoraTitleSearchView — a universal search sheet that queries all installed sources concurrently (capped 3), groups matches by source, and opens the chosen readable copy. Registered both new files via xcodeproj gem; added TRENDING/POPULAR/FIND_TO_READ* strings. BUILD SUCCEEDED.
- [x] NX-003 | Remove ComicK from defaults | area:ios | ✓ Removed parser:COMICK_FUN from SourceManager.defaultNyoraSources (first-launch pre-installs) AND from NyoraSourceRunner.recommended (its cover CDN is Cloudflare-blocked so covers never load). Left a comment marker in recommended. BUILD SUCCEEDED. | Remove parser:COMICK_FUN from SourceManager.defaultNyoraSources (cover CDN is Cloudflare-blocked). Leave it in the catalog/recommended only if still working; otherwise drop from recommended too.
- [x] NX-004 | Replace "More" tab with Settings | area:ios | ✓ The `.settings` NavSection already maps to SettingsView (gear + "SETTINGS" title), so the legacy "More" tab was already replaced. Completed the target destination set by dropping `.search` from NavConfig.defaultSections, leaving exactly Discover / Favourites (library) / Explore (browse) / History / Settings on a fresh install; search remains an opt-in section in NavConfigView and universal search will live in the Discover top bar (NX-005). BUILD SUCCEEDED.
- [x] NX-005 | Universal search at top (like android) | area:ios | ✓ Added an android-style tappable search bar at the top of the Discover feed (rounded pill, accent glass icon, "Search all sources" hint) plus an always-reachable magnifying-glass toolbar button in the nav bar. Both push the existing universal SearchViewController onto the Discover nav stack via the NavigationCoordinator, reusing the full aggregated search infra (results grouped by source, each source a section with a horizontal scroller + "View more"). Gave SearchViewController an autoActivateSearch flag (auto-focuses the field in viewDidAppear when opened this way). Added DISCOVER_SEARCH_HINT string. No new files (edited existing), so no xcodeproj change needed. BUILD SUCCEEDED.
- [x] NX-006 | Continue reading in Discover | area:ios | ✓ Added ContinueReadingSection.swift — an inline horizontal row at the TOP of the Discover feed (below the search bar, above the hero). Loads recent history (60 window), de-dupes to the most recent chapter per distinct manga (cap 15), resolves each into a full resume target (source + manga w/ chapters + last-read chapter) from Core Data. Each 140pt rail-style card shows the cover with a thin accent progress bar overlay + 2-line title; tapping opens SwiftUIReaderNavigationController at the last-read chapter. Refreshes on .historyAdded/.historySet/.historyRemoved/.updateHistory and self-hides when empty. Registered via xcodeproj gem (target Aidoku (iOS), Discover group). BUILD SUCCEEDED.
- [x] NX-007 | Remove iCloud sync | area:ios | ✓ Removed the iCloud/CloudKit sync feature end-to-end: dropped the ICLOUD_SYNC settings page + General.icloudSync toggle (Settings.swift); removed the General.icloudSync default and the Task.detached iCloud-availability probe + `import CloudKit` (AppDelegate.swift); converted CoreDataManager from NSPersistentCloudKitContainer to a plain NSPersistentContainer (deleted shouldUseiCloud, containerID, the General.icloudSync observer, and all cloudKitContainerOptions wiring) while keeping the Cloud/Local store descriptions + persistent-history/remote-change dedup path intact; removed General.icloudSync from BackupManager.excludedSettings. Nyora sync untouched. No new files. BUILD SUCCEEDED.
- [x] NX-008 | Start page (Login / Sign up / Guest) | area:ios | ✓ Added NyoraStartView.swift — a branded first-launch onboarding screen (accent gradient background, glass logo tile + "Nyora" wordmark) offering Sign in, Create account (both wired to NyoraSyncClient.signIn/register against stream.hasanraza.tech, with a fire-and-forget syncNow() on success), and Continue as guest (proceeds locally). Landing → auth-form mode switch with animated transition, inline error surfacing, busy spinner on the primary button. Presented full-screen (isModalInPresentation) over the TabBarController from SceneDelegate on first launch, gated + persisted via the "Nyora.completedStart" UserDefaults flag (NyoraStartView.completedKey) so it shows exactly once; wrapped in NyoraAccentTint so it follows the live accent. Registered via xcodeproj gem (target Aidoku (iOS), Sync group). BUILD SUCCEEDED.
- [x] NX-009 | Discover icon → trending chart | area:ios | ✓ Changed the Discover tab icon from "sparkles" to SF Symbol "chart.line.uptrend.xyaxis" in NavSection.systemImage (matches nyora-android), and updated the DiscoverView empty-state icon from "sparkles" to the same trending-chart symbol. No new files. BUILD SUCCEEDED.
- [x] NX-010 | Android: AniList feed for Discover | area:android | ✓ Verified nyora-android's Discover feed already sources entirely from AniList trending. DiscoverRepository.getMultiRails() fetches all rails in one aliased graphql.anilist.co request via AnilistRepository (trending/all-time-popular/top-rated/seasonal/new-releases + Action/Romance genre rails); DiscoverViewModel builds the hero from the top AniList trending media and renders every network rail from those AniList lists (source "Popular on <Source>" kept only as a trailing local rail). No source getHome/ComicK-first path remains. ./gradlew :app:compileDebugKotlin → BUILD SUCCESSFUL. Feature was already committed in nyora-android (commit 9b0d9d6); no new android code needed, so no android commit.
