# Nyora Batch Backlog

- [x] NX-001 | Theme/accent fix — presets & picker actually apply app-wide | area:ios | ✓ Added reactive AccentManager ObservableObject (republishes on .accentColorChanged) + NyoraAccentTint root wrapper; fixed .default accent to resolve to indigo (was systemPink); replaced accent-role NyoraTheme.indigo/Color.nyoraIndigo with dynamic accent across TabBar (live reconfigure), Discover, pill button, cards, reader toolbar, grid cell. BUILD SUCCEEDED. | The accent picker + 10 presets don't apply. ROOT CAUSE: (a) SwiftUI `Color.accentColor`/`.accentColor` reads the STATIC asset (fixed indigo) so preset changes never affect SwiftUI screens; (b) the design port HARDCODED NyoraTheme.indigo / Color.nyoraIndigo as the accent in restyled screens (tab bar, Discover, cards) so they ignore the selected accent. FIX: add a reactive dynamic accent the whole app follows — an ObservableObject (e.g. AccentManager: @Published var color, updated on Notification .accentColorChanged reading AccentColor.current) injected via .tint()/.environment at the SwiftUI roots (the UIHostingControllers / app entry), AND replace ACCENT-ROLE uses of NyoraTheme.indigo/Color.nyoraIndigo with the dynamic accent (AccentColor.current.color). Keep the existing window.tintColor wiring (AccentColorApplier in SceneDelegate). ACCEPTANCE: picking any preset (Miku etc.) changes tab/Discover/button tint live + persists across relaunch; default stays indigo.
- [ ] NX-002 | AniList feed for Discover (replaces ComicK-first) | area:ios | Feed the Discover screen from AniList instead of the first installed source. AniList GraphQL: POST https://graphql.anilist.co (JSON, NO auth). Query Page.media(type: MANGA, sort: [TRENDING_DESC] and a second [POPULARITY_DESC]) → id, title{english/romaji}, coverImage{large} (OPEN CDN s4.anilist.co → covers load), genres, description, siteUrl. Build a small AniList client + a Home (hero = top trending, rails = "Trending"/"Popular"). Wire DiscoverView to use it. Tapping an entry → run a universal search of that title across installed sources to open a reading source (or show a details sheet). Covers MUST render (open CDN).
- [ ] NX-003 | Remove ComicK from defaults | area:ios | Remove parser:COMICK_FUN from SourceManager.defaultNyoraSources (cover CDN is Cloudflare-blocked). Leave it in the catalog/recommended only if still working; otherwise drop from recommended too.
- [ ] NX-004 | Replace "More" tab with Settings | area:ios | Nav destinations should be Discover / Favourites / Explore / History / Settings (Settings replaces "More"). Update the tab controller + labels/icons.
- [ ] NX-005 | Universal search at top (like android) | area:ios | Add a top search bar (android-style, in the app bar of Discover/root) that runs a UNIVERSAL search across ALL installed sources concurrently and shows aggregated results (grouped by source, each source a section/row). Reuse existing search infra where possible.
- [ ] NX-006 | Continue reading in Discover | area:ios | Add a "Continue reading" section at the TOP of the Discover feed built from reading history (in-progress manga), horizontal row; tapping resumes the manga. Hide when empty. (There is already a ContinueReadingButton; this is an inline section.)
- [ ] NX-007 | Remove iCloud sync | area:ios | Remove the iCloud sync feature + its setting (General.icloudSync default + the iCloud sync code path / toggle in Settings). Keep the Nyora sync (stream.hasanraza.tech) intact.
- [ ] NX-008 | Start page (Login / Sign up / Guest) | area:ios | First-launch onboarding screen offering: Login, Sign up (both against the existing sync client / stream.hasanraza.tech — find NyoraSyncClient), and Continue as guest. Guest proceeds locally. Persist a "Nyora.completedStart" flag so it shows once. Present before the main tab UI on first launch.
- [ ] NX-009 | Discover icon → trending chart | area:ios | Change the Discover tab icon from the star/sparkles to SF Symbol chart.line.uptrend.xyaxis (matches nyora-android). Update the empty-state icon too if it uses sparkles.
- [ ] NX-010 | Android: AniList feed for Discover | area:android | Update nyora-android's Discover/feed to source from AniList trending (android may already have an AniList recommendation source — reuse it). Build with: cd /Users/hasanraza/Desktop/kotatsu/Nyora/nyora-android && ./gradlew :app:compileDebugKotlin 2>&1 | grep -E "BUILD SUCCESSFUL|BUILD FAILED|error:|e: " | head -20. Commit in the nyora-android repo.
