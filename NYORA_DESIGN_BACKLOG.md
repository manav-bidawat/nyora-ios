# Nyora iOS Design Backlog (Phases 2-7)

Porting nyora-android's UI design natively into Aidoku (iOS). Phase 1 (Poppins + indigo accent + NyoraTheme tokens) is DONE. Each item is atomic and independently buildable. Reuse `iOS/New/NyoraTheme.swift` tokens; never break navigation or data flow.

Build check: `cd /Users/hasanraza/Desktop/kotatsu/Nyora/Aidoku && xcodebuild -project Aidoku.xcodeproj -scheme "Aidoku (iOS)" -sdk iphonesimulator -configuration Debug -destination 'generic/platform=iOS Simulator' -clonedSourcePackagesDirPath /tmp/aidoku-spm -skipPackagePluginValidation build`

## Phase 2 — Reusable style primitives (foundational, low-risk)
- [x] ND-001 | Nyora pill ButtonStyle (fully-rounded, indigo fill / white variant) | area:library | android:res/values/styles.xml Widget.Nyora.Button.Pill | target:iOS/New/Utilities/ButtonStyles/NyoraPillButtonStyle.swift | new SwiftUI ButtonStyle renders a cornerPill capsule with Poppins SemiBold label; compiles and previews ✓ .nyoraPill / .nyoraPillWhite Capsule styles w/ Poppins SemiBold, press scale; registered + BUILD SUCCEEDED
- [x] ND-002 | Tinted-card ViewModifier (flat, surface tint + 1px outline, cornerCard) | area:library | android:res/values/styles.xml Widget.Nyora.Card.Tinted | target:iOS/New/Utilities/NyoraCardStyle.swift | `.nyoraTintedCard()` modifier applies slateOnSurface fill, 1px indigo-tint border, 20pt corners, 0 shadow ✓ NyoraTintedCardModifier + .nyoraTintedCard(cornerRadius:padding:); adaptive tinted surface (slate 100/800) + 1px indigo outline, 0 shadow; registered + BUILD SUCCEEDED

## Phase 3 — Library grid restyle
- [x] ND-003 | Library grid card restyle to tinted card + 12pt cover | area:library | android:item_manga_grid.xml | target:iOS/New/Views/Common/MangaGridItem.swift | grid item uses nyoraTintedCard, cover clipped 13:18 at cornerCover, Poppins SemiBold title; grid still scrolls/taps ✓ cover now clips at cornerCover (12pt) w/ nyoraCardOutline 1px border, title is Poppins SemiBold; kept self-contained 2:3 overlay-title tile (callers e.g. MigrateResultsView force .aspectRatio(2/3) so title-below would break layout); BUILD SUCCEEDED
- [x] ND-004 | Library cover unread badge + reading-progress overlay | area:library | android:item_manga_grid.xml badge/progress | target:iOS/UI/Common/Manga/MangaGridCell.swift | unread count badge (indigo pill) top-right and bottom progress bar overlay render on cover without clipping ✓ added a 4pt bottom reading-progress bar overlay (dark track + NyoraTheme.indigo fill driven by ReadingProgress.fillFraction) on the grid cover, hidden on reuse/invalid; DoubleBadgeView unread badge is now a fully-rounded indigo pill (cornerRadius 10, tintColor=Nyora indigo). Kept badge at top-left (bookmark ribbon owns top-right) to avoid layout collision; BUILD SUCCEEDED

## Phase 4 — Manga details restyle
- [x] ND-005 | Details cover + title block restyle (left cover ~32%, 16pt, Poppins headline) | area:details | android:activity_details.xml header | target:iOS/New/Views/Manga/MangaDetailsHeaderView.swift | left-aligned cover at cornerCover 13:18, title uses Poppins bold headline; existing blurred backdrop preserved ✓ left cover now uses 13:18 coverAspectRatio at cornerCover (12pt) with 1px nyoraCardOutline + soft elevation shadow; title switched to Poppins Bold 22; 114pt width (~32% of content) and blurred backdrop untouched; BUILD SUCCEEDED
- [x] ND-006 | Details filled info-table card (Source/Author/Rating/State/Chapters/Progress rows) | area:details | android:activity_details.xml info card | target:iOS/New/Views/Manga/MangaDetailsHeaderView.swift | tinted card (20pt, 1px outline) with label/value rows and inline progress bar renders below title ✓ new infoTableView renders a nyoraTintedCard below the title block with Poppins label/value rows (Source/Author/Rating/State→STATUS/Chapters) plus a Progress row carrying an inline ProgressView bar + percent; replaced the standalone progressView call with it; added SOURCE/RATING/PROGRESS en strings (STATUS reused); BUILD SUCCEEDED
- [x] ND-007 | Details description More + genre chips restyle | area:details | android:activity_details.xml chips | target:iOS/New/Views/Manga/ExpandableTextView.swift | description shows Poppins body with More toggle; genres render as cornerPill tinted chips ✓ ExpandableTextView markdown paragraph now uses explicit Poppins-Regular FontFamily; MORE toggle is Poppins SemiBold 12 in Nyora indigo; genre chips (TagView) restyled to fully-rounded Capsule with 12%-indigo tinted fill + 1px nyoraCardOutline + Poppins Medium 13 indigo label; taps into genre filter unchanged; BUILD SUCCEEDED
- [x] ND-008 | Chapter rows as outlined cards (16pt corners, 1px outline) | area:details | android:item_chapter.xml | target:iOS/New/Views/Manga/ChapterTableCell.swift | each chapter cell draws an outlined card at cornerCover with Poppins title/subtitle; selection + read state still work ✓ ChapterTableCell now wraps its row content in a flat 16pt-corner card with a 1px nyoraCardOutline stroke (no fill/elevation), 14/10 inner padding + 16/4 outer margin so cards stand apart; title→Poppins SemiBold 15, subtitle→Poppins 13 (read/locked still dims to secondary); added .listRowSeparator(.hidden) in MangaView.viewForChapter so the default list dividers no longer double up with the card outlines; taps/selection/download/lock states unchanged; BUILD SUCCEEDED

## Phase 5 — Explore restyle
- [x] ND-009 | Explore search-hero pill card | area:explore | android:item_explore_search_hero.xml | target:iOS/UI/Browse/BrowseViewController.swift | large pill search card (cornerPill, tinted) at top of Browse launches search; tapping opens existing search flow ✓ new NyoraSearchHeroView (UIControl) — full-width fully-rounded pill (capsule via cornerPill/height) with flat slate tinted surface + 1px indigo outline, leading magnifier glyph + Poppins "SEARCH" placeholder, press scale/alpha; installed as BrowseViewController.tableView.tableHeaderView (sized 72pt in viewDidLayoutSubviews) and tapping becomeFirstResponder's the existing UISearchController search bar; BUILD SUCCEEDED
- [x] ND-010 | Explore quick-actions 2x2 tonal card (Local/Bookmarks/Random/Downloads) | area:explore | android:item_explore_buttons.xml | target:iOS/New/Views/Browse/QuickActionsCard.swift | new SwiftUI 2x2 grid of tall tonal buttons in a tinted card wired to existing Local/Bookmarks/Random/Downloads actions ✓ new QuickActionsCard SwiftUI view: flat nyoraTintedCard holding a 2×2 grid of tall (68pt) tonal tiles (indigo 12% fill + 1px nyoraCardOutline, SF icon + Poppins SemiBold 13 indigo label, press scale/opacity). Hosted below NyoraSearchHeroView in a combined UIView tableHeaderView (sized in viewDidLayoutSubviews). Wired to real, safe actions in BrowseViewController: Local→push local source (fallback add-source), Bookmarks→switch to Library/Favourites tab, Random→push a random installed non-local source, Downloads→present DownloadQueueView. Added BOOKMARKS/RANDOM en strings; registered via xcodeproj gem; BUILD SUCCEEDED
- [x] ND-011 | Source rows as rounded tinted cards (favicon + title + subtitle) | area:explore | android:item_explore_source_list.xml | target:iOS/New/Views/Browse/SourceTableCell.swift | each source row is a tinted rounded card showing favicon, Poppins title, subtitle; navigation to source unchanged ✓ SourceTableCell now wraps favicon + title/subtitle in a flat .nyoraTintedCard (slate tint + 1px indigo outline, 20pt corners, 14pt inner padding); title→Poppins SemiBold 16, version/language subtitle→Poppins 13, NSFW badge→Poppins Medium 10. Its one consumer (SourceSettingsView header) switched to a clear listRowBackground + 8pt insets so the tinted card is the visible surface (no double card). Content-rating badge + navigation untouched; BUILD SUCCEEDED

## Phase 6 — Discover home (signature screen)
- [x] ND-012 | Discover screen scaffold (new SwiftUI screen fed by source getHome) | area:discover | android:fragment_discover.xml | target:iOS/New/Views/Discover/DiscoverView.swift | new vertical-feed DiscoverView compiles, loads home data via existing NyoraRunner/source home, shows loading + empty states ✓ new DiscoverView SwiftUI screen with a LoadState enum (loading/loaded/empty/failed); .task picks the first installed source whose features.providesHome and awaits source.getHome(), rendering a circular ProgressView while loading, a sparkles+Poppins empty state (no home source or no components), an ErrorView with retry on failure, and a placeholder vertical feed of nyoraTintedCard section rows proving the home data is wired in (hero/rails/pager come in ND-013..015). Added DISCOVER / DISCOVER_EMPTY_MESSAGE / DISCOVER_SECTION_COUNT en strings; not yet tabbed in (ND-016); registered via xcodeproj gem; BUILD SUCCEEDED
- [x] ND-013 | Discover HERO card (full-bleed cover, L→R gradient, title+genres+white Read pill) | area:discover | android:item_discover_hero.xml | target:iOS/New/Views/Discover/DiscoverHeroCard.swift | ~240pt hero at cornerHero with dark left gradient, bottom-left Poppins bold title + genres + white Read pill (ND-001) opening details ✓ new DiscoverHeroCard: 240pt full-width card at cornerHero (24pt) with full-bleed SourceImageView cover, a left→right dark LinearGradient + a bottom darkening gradient, bottom-left Poppins-Bold 24 title, up to 3 genre labels (Poppins Medium 12, bullet-joined), and a white "Read" pill (black label, Capsule). Whole card taps through to MangaViewController via NavigationCoordinator (matching Android hero). Wired into DiscoverView.loadedView via a new Home.heroManga helper (first manga across bigScroller/scroller/mangaList/mangaChapterList components, resolving Link.value). 1px nyoraCardOutline; BUILD SUCCEEDED
- [x] ND-014 | Discover horizontal RAIL (section title + 140pt bordered cards) | area:discover | android:item_discover_rail.xml,item_discover_rail_card.xml | target:iOS/New/Views/Discover/DiscoverRailView.swift | section header + horizontal scroller of 140pt cornerCover bordered cards (cover + 2-line title); taps open details ✓ new DiscoverRailView: Poppins-SemiBold 18 section title over a horizontal ScrollView of 140pt-wide flat cards (16pt corners, nyoraCardSurface fill + 1px nyoraCardOutline). Each card = full-bleed 13:18 SourceImageView cover (height derived from coverAspectRatio) + 2-line Poppins-SemiBold 13 title, whole card taps to MangaViewController via NavigationCoordinator. DiscoverView.loadedView now renders a rail per component via a HomeComponent.Value.railManga helper (bigScroller/scroller/mangaList/mangaChapterList → [Manga], skipping non-manga components), replacing the ND-012 placeholder count cards; registered + BUILD SUCCEEDED
- [x] ND-015 | Discover recommendation pager with dots indicator | area:discover | android:item_recommendation.xml,item_recommendation_manga.xml | target:iOS/New/Views/Discover/DiscoverRecommendationPager.swift | swipeable TabView pager of recommendation cards with indigo dots indicator; compiles and pages ✓ new DiscoverRecommendationPager: a paging TabView (.page indexDisplayMode:.never) of recommendation cards — each a nyoraTintedCard with a 13:18 cornerCover thumbnail + Poppins-SemiBold title + Poppins description/authors subtitle, tapping to MangaViewController via NavigationCoordinator. Below it a custom DotsIndicator (indigo capsules, active dot grows to a pill w/ ease animation) mirroring Android's DotsIndicator. DiscoverView now renders the featured `bigScroller` component as this pager (new HomeComponent.Value.isBigScroller helper) while other scroller/list components stay ND-014 rails; registered via xcodeproj gem; BUILD SUCCEEDED
- [x] ND-016 | Register Discover as a tab destination | area:nav | android:activity_main.xml nav | target:iOS/UI/Common/TabBarController.swift | Discover appears as a tab hosting DiscoverView without breaking existing Library/Browse/History/Search tabs ✓ added a `.discover` case to NavSection (title→DISCOVER, systemImage→"sparkles") and made it the first entry in NavConfig.defaultSections (matching Android where Discover leads). TabBarController.setUpTabs now builds a discoverViewController = NavigationController wrapping a UIHostingController(DiscoverView) with its own NavigationCoordinator env object (same pattern as History), routed via the viewController(for:) switch and given prefersLargeTitles. DiscoverView gained a .navigationTitle("Discover"). Works on both the iOS 26 UITab path and the pre-26 UITabBarItem path; existing Library/Browse/History/Search/Settings tabs untouched; BUILD SUCCEEDED

## Phase 7 — Nav + Reader chrome
- [x] ND-017 | Rename tab destinations to Discover / Favourites / Explore / History | area:nav | android:res/values/strings.xml nav labels | target:iOS/UI/Common/TabBarController.swift | tab titles/labels read Discover, Favourites, Explore, History; underlying view controllers unchanged ✓ NavSection.title now maps .library→NAV_FAVOURITES ("Favourites") and .browse→NAV_EXPLORE ("Explore") while keeping Discover/History, so both the iOS 26 UITab path and the pre-26 UITabBarItem path (which read section.title) show the Nyora nav labels; underlying LibraryViewController/BrowseViewController and NavConfigView untouched. Added NAV_FAVOURITES/NAV_EXPLORE en strings rather than reusing the LIBRARY/BROWSE keys (still used as real screen titles elsewhere); BUILD SUCCEEDED
- [x] ND-018 | Floating rounded pill tab bar appearance | area:nav | android:navigation_rail_fab.xml | target:iOS/UI/Common/TabBarController.swift | tab bar uses a floating rounded pill appearance (indigo selected tint, Poppins) without clipping items or breaking taps ✓ new configureFloatingTabBar() applies a UITabBarAppearance with indigo selected icon+title tint (secondaryLabel unselected) and Poppins Medium/SemiBold item fonts across stacked/inline/compact layouts, plus tabBar.tintColor=indigo. On iOS 26 it keeps the native liquid-glass floating bar (retint only); pre-26 it uses a transparent bar appearance and inserts a floating rounded pill (systemChromeMaterial blur, 1px indigo-18% outline, cornerRadius=min(h/2,20)) at subview index 0, frame-laid in viewDidLayoutSubviews with 12pt side insets so the bar reads as a detached capsule without moving/clipping the item buttons or changing hit-testing; BUILD SUCCEEDED
- [x] ND-019 | Detached circular "Continue reading" button | area:nav | android:navigation_rail_fab.xml FAB | target:iOS/New/Views/Common/ContinueReadingButton.swift | new circular indigo button overlays the tab area, hidden when no in-progress manga, opens the last-read chapter ✓ new ContinueReadingButton: a floating 58pt circular button with an indigo→purple gradient fill, white book glyph, 1px inner highlight + indigo drop shadow and press-scale feedback. Self-contained: it resolves the most recent reading-history entry via CoreDataManager.getRecentHistory(limit:1) into a ResumeTarget (source + manga w/ full chapter list + last-read chapter matched by chapter.key), self-hides (animated) when nothing is resumable, refreshes on historyAdded/historySet/historyRemoved/updateHistory, and on tap opens the chapter through the existing SwiftUIReaderNavigationController in a fullScreenCover. Overlaid bottom-trailing on DiscoverView (the Nyora home) above the tab bar. Reused CONTINUE_READING string; registered via xcodeproj gem; BUILD SUCCEEDED
- [x] ND-020 | Reader centered title+subtitle bar + floating rounded bottom action bar | area:reader | android:activity_reader.xml | target:iOS/UI/Reader/ReaderToolbarView.swift,iOS/UI/Reader/ReaderSliderView.swift | top bar shows centered Poppins title+subtitle; bottom action bar is a floating rounded pill with prev/next flanking the page slider; controls still function ✓ top bar: UINavigationItem.setTitle (reader-only consumer) now stacks a centered Poppins-SemiBold 15 title over a Poppins-Medium 11 secondary subtitle. Bottom bar: ReaderToolbarView gained a floating capsule pill (systemChromeMaterial blur + 1px indigo-18% outline, cornerRadius=h/2 in layoutSubviews) that houses the existing ReaderSliderView flanked by new prev/next chapter UIButtons (chevron.left.2/right.2, indigo tint); sliderView stays a direct subview so the outside-bounds thumb hitTest still works. Page/incognito/pages-left labels restyled to Poppins and moved below the pill (toolbar customView height 40→48). ReaderViewController wires the pill buttons to previousChapter/nextChapter, removed the duplicate navbar chevrons, and drives button visibility from Reader.controls (.prevChapter/.nextChapter) via setChapterButtons; setSliderVisible unchanged. No new files; BUILD SUCCEEDED
